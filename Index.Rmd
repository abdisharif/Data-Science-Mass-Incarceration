---
title: "index"
output: html_document
---

## Install Packages
```{r}
install.packages("ggplot2")
install.packages("reshape2")
install.packages("dplyr")
install.packages("lintr")
```

## Load Libraries
```{r}
library(dplyr)
library(ggplot2)
library(reshape2)
library(lintr)
```

## Summary

```{r}
source("summary.R", echo = T, prompt.echo = "", spaced = F)
```

## Table

```{r}
source("Table.R", echo = T, prompt.echo = "", spaced = F)
```

## First plot

```{r}
source("first_chart.R", echo = T, prompt.echo = "", spaced = F)
```

## 2nd plot

```{r}
source("2nd_chart.R", echo = T, prompt.echo = "", spaced = F)
```

## 3rd plot

```{r}
source("3rd_chart.R", echo = T, prompt.echo = "", spaced = F)
```

